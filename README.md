# GamePulse API (Gaming News & Reviews)

Node.js + Express + MongoDB backend.

## Features
- JWT auth (register/login)
- User profile (get/update)
- Reviews CRUD (only owner can edit/delete)
- Gaming news from external API (NewsAPI)

## Setup
```bash
npm install
